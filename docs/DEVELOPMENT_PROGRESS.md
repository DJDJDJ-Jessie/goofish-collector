# 闲鱼公开商品研究采集器｜开发进度

更新时间：2026-08-16
当前版本：0.6.5 开发中（`codex/monitoring`，已同步 `main` 最新采集修复）

## 1. 当前结论

产品已经从“弹窗里手工点一下”的原型，进入“侧边栏承载长期任务、Service Worker 负责自动编排”的重构阶段。当前最重要的修复不是再给按钮加一个提示，而是让 UI 与任务生命周期解耦：任务启动后由后台继续逐个打开详情页，界面只负责控制和展示。

本轮 0.4 重点是把侧边栏改成分层工作台：主页只展示功能入口，采集参数进入独立页面，任务处理和完成结果进入独立任务页，数据导出进入数据中心。同时移出了导致 Chrome 拒绝重新加载的诊断目录。

本轮 0.4.1 针对真实搜索页截图继续修复：搜索按钮被任务状态轮询误判为禁用、闲鱼分页没有“下一页”文字按钮、返回按钮历史栈不稳定，以及首页说明内容挡住主要入口。

本轮 0.5.0 根据实际导出 Excel 和评价诊断包继续修复：搜索任务必须真的进入详情页；商品主表改为用户指定的 18 列；修正账号页把店铺名称识别成“登录”的问题；新增当前店铺页、逐条评价和评价图片采集，并把店铺资料/评价拆成独立工作表。

本轮 0.5.0 收尾又修正了图片预算和评价懒加载：商品图片上限只作用于商品图，已加载的评价图片独立完整处理；评价区如果使用内部滚动容器，会优先滚动该容器；主表在第一张商品图失败时会自动选择同商品下一张成功图片。

本轮 0.5.0 又根据实际导出的 Excel 回归修正了好评率、粉丝/关注和店铺页直接导出；随后发现“店铺简介为 46”等问题不是数字格式问题，而是账号页异步渲染与宽泛 DOM 选择器造成的字段串位。

本轮 0.5.1 改为修复根因：账号资料必须在连续两次读取一致后才合并；简介只从账号资料区域的明确简介节点读取；商品数/评价数只从对应语义 Tab 读取；若已有明确 Tab 标题但没有其自身数量，则保留空值，不从整页随机数字补位。纯数字店铺简介会原样保留。

本轮 0.5.1 继续补齐详情属性：服务类商品的“服务类型”（例如“金融”）优先写入唯一的“类目”字段；商品图片或属性图片不作为统一字段来源。

本轮 0.5.2 根据《搜索页逐个详情页.xlsx》和店铺页截图追查到两个此前没有解决的根因：账号页的 `infoTop` 只包含昵称，旧实现把它误当成完整资料区，导致地区、粉丝、关注、简介全部落空；详情 DOM/API 合并时空值可能覆盖有效值，且详情属性标签会拆成单字，导致服务类型回退成内部 `categoryId`。本轮修复账号资料最小容器、拆字标签解析、非空合并和语义类目优先级，并新增当前店铺历史状态查询。

本轮 0.5.3 针对最新回归截图追查到任务生命周期问题：搜索任务显示“详情页 3/4、75%”时，后台正在等待第 4 个详情页的页面消息；旧消息监听器/接口观察异常没有调用回调，导致 `collecting` Promise 永久等待。旧实现把单个活动任务错误地当成全局锁，于是当前详情、店铺页、批量链接和搜索入口同时变成不可点击，看起来像四个独立故障。修复包括后台/侧边栏/内容脚本消息超时、API 观察和详情采集异常兜底、旧任务自动恢复，以及对“ready-to-collect”漏闹钟的补排程。

本轮 0.5.4 根据最新 Excel 和“当前详情页/当前店铺页”反馈修复了三类根因：原开店时长正则在“1天前来过 | 来闲鱼239天”中用宽范围邻近匹配，可能错误捕获 1 天，现改为标签和值直接配对；简介和服务类型不再依赖整页随机数字或固定 class，而是在账号资料/详情属性语义块内识别，纯数字简介仍然合法；商品任务结果与店铺资料彻底隔离，任务先进入 `stagedItems`，当前详情、批量和搜索任务各自提供“加入数据中心商品表”和“单独导出”入口，店铺导出生成不含商品表的独立工作簿。当前详情页直接复用已打开的标签页，不再为补店铺资料新开标签页。

本轮 0.5.4 收尾又补了两个兼容性问题：账号资料作用域兼容 `description--*` 简介容器；旧版本任务没有 `stagedItems` 时，完成任务仍可按历史采集数量显示商品导出入口。当前详情/API 采集返回给侧边栏的结果改为使用后台已经按当前商品 ID/URL过滤后的记录，避免推荐商品混入“导出当前详情页”。

本轮 0.5.5（历史实现记录）针对最新 `1.xlsx`、`3.xlsx` 和实际操作反馈继续修复：商品任务不再在详情采集后直接结束，而是强制执行“详情页 → 卖家账号页 → 下一商品/结束”的阶段，最后一条也必须完成店铺补采集；当前详情页的补采集复用原标签页并返回，不创建临时卖家标签页。当时商品工作簿曾包含图片索引和说明，商品多图曾写入图片索引；后续 0.5.9/0.6.0 已取消商品图片索引表，改为在商品表同一行展开图片列。店铺工作簿、首页数据中心和真实图片嵌入等内容也以当前版本的说明为准。

本轮 0.5.6 针对“商品页明明有卖家账号入口，但商品表的卖家账号页仍为空”继续修复：真实 Chrome 商品页的卖家昵称位于一个 `item-user-info-nick--*` 节点内，其上层是带 `target="_blank"` 的 `/personal?userId=...` 链接；采集器现在优先从昵称节点向上找父级链接，再按卖家统计文本做兜底发现。详情页、批量链接和搜索跨页在结果缺少 `sellerUrl` 时都会重新向当前详情页查询 `GET_SELLER_ENTRY`，不会因为接口结果缺字段而跳过卖家补采集；当前详情入口也会对没有 `sellerUrl` 的有效商品结果发起补采集。代码级任务/Excel 烟测通过，真实 Chrome 的页面脚本读取在本轮出现超时，仍标记为待验收，不把超时误报为通过。

本轮 0.5.7 根据 `1.xlsx`、`2.xlsx` 和登录提示截图追到四个不同根因：搜索任务曾把 DOM 卡片与网络缓存按到达顺序混合，导致访问顺序不等于页面视觉顺序；详情页只等待浏览器加载完成，没有处理置顶登录对话框和延迟渲染，容易在字段尚未出现时读空值；当前详情补采集账号页往返及 URL 跟踪参数变化会触发侧边栏清空暂存结果，形成“提示成功但按钮变灰”；店铺工作簿虽然实际含有 78 条评价和 5 条评价图片记录，但默认打开第一张“店铺资料”表，用户很难看到评价。修复后搜索链接按屏幕纵横坐标逐排排序，网络只补同一可见商品字段；四类入口统一关闭公开登录提示并等待页面稳定；当前详情以真实结果行为成功标准并按商品 ID 保持按钮；有评价的店铺 Excel 默认打开“店铺评价”并在完成提示中明确评价/图片数量。

本轮 0.5.8 根据你的使用反馈调整导出形态：之前把店铺资料、店铺评价、评价图片拆成三个工作表，虽然内部结构清晰，但不利于逐行对照同行。现在导出时将三类数据合成为“店铺综合”一张数据表：一行一条评价，重复写入店铺资料字段，评价图片文件名、图片状态、失败地址和真实图片对象全部放在对应评价行；没有评价时保留一行店铺资料。工作簿只保留“店铺综合”和“说明”两张表，下载提示和数据中心说明同步更新。

本轮 0.5.9 根据你的进一步澄清修正导出结构：店铺资料不应与评价字段重复在同一张表。现在店铺工作簿严格只有两张数据表：“店铺资料”一店一行；“店铺评价综合”一条评价一行，并把该评价的图片文件名、下载状态、失败地址和真实图片嵌入同一行。工作簿不再额外生成“说明”页，默认打开店铺资料表，界面提示和数据中心下载名称同步更新。

本轮 0.5.10 只修复停止任务的异步竞态：点击停止时先登记任务取消令牌，再写入终止状态、清除任务闹钟并关闭采集专用标签页；已经在途的页面消息、导航回调和旧闹钟在返回后会校验任务 ID/状态，不能再把任务恢复成 `collecting` 或 `ready-to-collect`。原始搜索页不关闭，已采集的暂存数据和历史快照保留；侧边栏增加“正在停止”反馈，避免用户误以为按钮没有生效。

本轮 0.5.11 根据最新店铺导出 `3.xlsx` 和商品详情截图修正导出字段边界：商品主表新增“浏览数、想要数”，紧跟在商品文案后，并从详情页的“浏览/想要”标签或当前详情公开响应读取；店铺资料表移除“开店时长、商品好评率”，不再把店铺评价数量换算成百分比，也不再从商品补采字段混入店铺表。保留纯数字店铺简介的语义识别，不用字段格式限制来掩盖识别错误。

本轮 0.6.0 新开 `codex/monitoring` 分支，并使用独立工作目录 `D:\CodeSpace\goofish-collect-monitoring` 开发，稳定版 `main` 仍保留在 `D:\CodeSpace\goofish-collect`。新增商品监控和店铺监控两个入口：支持保存多条同类型链接、每日开始时间和 Chrome 默认下载目录下的相对子文件夹；配置写入本地后建立独立每日闹钟。监控每次逐条打开链接并复用页面稳定等待、公开登录提示处理、详情/账号页校验和真实图片导出；商品监控自动下载商品工作簿，店铺监控自动下载“店铺资料+店铺评价综合”工作簿，不自动混入数据中心。监控运行状态单独保存在 `xianyu_monitor_runs_v1`，不占用普通任务锁。
## 1.1 本轮 0.5.12：字段缺失、异常交互数和失败链接

本轮以用户提供的 `8.9商品表.xlsx` 为证据做了回归分析。确定的异常不是 Excel 自己把正常数量变成小数，而是详情接口中的无单位小数被旧版当作“想要数”直接保存；同时，详情页在卖家入口、服务类型和账号资料尚未完成挂载时就被接受，造成半成品行、类目空缺、开店时长错位或店铺字段为空。

已完成的修复：

- 在 `content.js`、`background.js` 和 `xlsx.js` 三层统一交互数清洗：整数保留，`5.5万` 转成 `55000`，`55.00827`/`52.50106`/`245.00204` 这类无单位小数拒绝写入，不能覆盖页面已经读到的整数。
- 详情页增加完整性门槛、API 多轮快照合并和卖家入口重试；卖家入口只接受页面真实公开的账号页 URL 或账号 ID，不按昵称猜地址。
- 修复商品账号补采 profile 的字段边界：店铺总表仍不写“开店时长/商品好评率”，但商品任务从账号页补采时保留 `239天`、`100%` 等商品行字段；纯数字店铺简介仍按真实文本保存。
- 服务类型/服务类目/服务类别统一作为商品唯一“类目”字段的优先来源；长纯数字 `categoryId` 仅视为内部 ID，不导出为可读类目。
- 任务新增 `failures`、`sellerFailures`、`qualityWarnings` 统一展示和历史保存；详情失败、卖家资料失败、字段待补充会在任务页列出 URL/ID/原因，可复制失败链接重新采集。存在成功结果但有未完成项时显示 `partial`，自动下载仍会导出并保留告警。

本轮回归已通过：

- `node --check background.js content.js xlsx.js sidepanel.js`
- `tests/product-export-shape-smoke.mjs`
- `tests/store-export-shape-smoke.mjs`
- `tests/stop-job-smoke.mjs`
- `tests/ui-order-smoke.mjs`
- `git diff --check`

下一步只需要在 Chrome 扩展管理页重新加载稳定版，并重新跑一组批量商品链接或搜索跨页任务；如果仍有某一条链接字段缺失，任务页现在会先显示该链接和缺失字段，届时提供该链接、截图和导出的 Excel 即可，不需要再导出临时诊断包。

## 1.2 本轮 0.5.13：修复“第一个数字/第一个容器”抢占字段

针对上一轮之后仍可能出现的字段缺失，本轮继续修复两个更隐蔽的解析问题：

- 详情页交互数不再只取整块文本中遇到的第一个候选。现在优先读取带 aria-label、title 等语义属性的可见节点，再按 innerText 和整块文本逐个扫描；每个候选都经过整数或“万/w”语义校验，无单位小数被跳过后会继续寻找页面真正显示的整数。
- 类目不再只检查第一个 item-info 容器，也不再把所有带 category 的链接混成类目。现在会按详情主信息、详情属性、页面根节点逐个尝试服务类型标签，并仅使用明确面包屑或标签行做回退；长纯数字 categoryId 继续留空。
- 卖家入口的公开账号 URL 解析范围扩展到昵称附近的 data-url、data-href、data-link、aria-label、title、onclick 节点，仍然只接受真实 personal?userId=... 地址或页面公开的数字账号 ID。

本轮新增回归断言：异常交互小数必须为空、整数和 5.5万 必须保持正确；商品账号补采仍保留纯数字店铺简介、开店时长和商品好评率。

## 1.3 本轮 0.5.14：明确字段缺失告警的含义

本轮不改变现有采集结果和导出字段，只补充“缺失字段到底代表什么”的用户解释：

- `failures` 表示详情页或账号页流程真正失败；`qualityWarnings` 表示任务结束时没有读到预设字段。
- `qualityWarnings` 不能仅凭“没有读到”断言闲鱼一定没有该字段。字段可能没有公开、尚未完成懒加载，或者页面结构已变化。
- 只有页面明确出现“暂无”“未公开”等否定状态时，才能把它解释为页面未公开；其他情况继续显示“字段待补充”，由用户根据页面核验。
- 任务页的“未成功链接”说明文字已同步更新，避免把质量告警误解为商品链接一定失败。

## 1.4 本轮 0.6.0：任务中心、店铺全商品、高清图片与字段工作台

本轮在不改动监控分支的前提下，继续完善主分支的长期任务能力：

- 任务从“单一当前任务”升级为持久化任务集合。每个任务有独立 ID、采集标签页、闹钟、暂存结果和历史快照；返回首页、关闭侧边栏或启动其它任务后，仍可在任务中心查看、暂停/继续、停止和导出指定任务。
- 新增“采集店铺全部商品详情”：从店铺/账号页按页面视觉顺序发现公开商品链接，再逐个进入商品详情页，并执行卖家资料补采；结果属于商品任务，不与店铺资料/评价表混合。
- 详情长文案会在正文候选范围内自动点击“展开/展开全文/查看更多”等控件后重新读取；优先使用正文节点，只有没有可读正文时才使用分享摘要，避免平台提示文本抢占商品文案。
- 商品多图不再生成图片索引表，而是在商品表同一行展开“商品图片、商品图片2、商品图片3…”列并嵌入真实图片；店铺评价图片仍与评价文本放在“店铺评价综合”同一评价行。
- 图片下载优先尝试页面公开的原图地址和去除缩略图参数后的地址。旧版约 1600 像素/8MB 的统一压缩已取消，只有图片格式不兼容或超过 8192 像素/24MB 保护上限时才转码，转码质量为 0.96。
- 设置页新增字段配置工作台，分别维护商品表、店铺资料表和店铺评价表的字段选择与导出顺序；所有采集入口和数据中心导出复用已保存配置。
- 并发任务写入增加串行队列，避免两个任务同时保存时互相覆盖；店铺全商品任务也正式进入详情结果暂存和导出路径。

## 1.5 本轮 0.6.1：店铺商品导航、完成态和商品图片布局

本轮针对“采集店铺所有商品详情”失败、当前页采集完成提示过弱、单图重复嵌入和店铺按钮含义不清四类问题做根因修复：

- `store-products` 任务发现的商品链接是对象，不能直接作为 `tabs.update({ url })` 的参数；新增统一链接归一化，导航、详情匹配、商品匹配和失败记录都只使用真实字符串 URL，避免 `expected string, found object` 和 `[object Object]`。
- 当前详情页和当前店铺评价页新增独立的圆形“采集完成”卡片；完成后保留“加入数据中心”和“单独导出/立即下载”按钮，失败时不显示假完成。
- 商品图片在导出前按 CDN 缩略图/尺寸参数和真实图片二进制双重去重；商品只有一张逻辑图片时只生成一个主图单元格。真实多图保留主图在配置位置，其余图片列追加到商品表最后，防止不同商品图片数量造成字段错位。
- 可见入口由“采集当前店铺页”改为“采集当前店铺评价”，并同步更新页面标题、提示和文档。

## 1.6 本轮 0.6.2：导出命名、二进制图片去重与字段目录清理

本轮依据 `D:/GoogleDownload/小林爱写作8.15.xlsx` 做了导出包级检查。该文件的 `商品数据` 表中有 13 个真实嵌入媒体，其中 3 组媒体二进制完全相同，重复出现在两个不同商品行的相同图片位；这说明旧版只按 URL 去重不足以消除 CDN 地址变化或跨商品复用造成的媒体重复。

已完成：

- 导出文件名改为固定规则：商品任务/数据中心商品表为 `商品YYYYMMDD-HHmm.xlsx`；店铺评价导出为 `店铺评价-店铺名-YYYYMMDD-HHmm.xlsx`；店铺全部商品导出为 `商品表-店铺名-YYYYMMDD-HHmm.xlsx`。下载子文件夹仍由设置控制，文件名模板输入已移除，避免设置与产品规则冲突。
- `offscreen.js` 在下载结果层按商品/评价作用域进行 URL + 图片二进制指纹去重，并重新生成图序号；`xlsx.js` 在媒体分配和 drawing 前再做一次二进制去重。不同商品共用同一张图时只复用 ZIP 内媒体文件，不会把一个商品的图片列挪到另一个商品。
- 商品字段工作台移除发布时间、商品评价摘要、店铺评价摘要和评价示例等不稳定辅助字段；这些字段不再被当成可选商品列，评价统一在店铺评价综合表中查看。内部解析仍可保留评价信息作为好评率补充来源，不影响现有采集逻辑。
- 增加文件命名、字段目录和相同二进制图片回归检查，已有商品/店铺/任务中心烟测继续通过。

## 1.7 本轮 0.6.3：任务/历史清理、历史重采集状态与导出兜底

本轮针对“功能入口含义不完整、任务和历史只能逐条处理、已有记录仍显示首次采集、保存对话框出现 UUID、图片 2/3 仍可能重复”继续收口：

- 入口名称统一为“采集当前店铺数据”，页面内保留两个明确动作：采集当前店铺评价、采集店铺全部商品详情；后者仍作为独立商品任务进入任务中心。
- 任务中心增加“清空全部任务”。后台先登记取消令牌，再清除任务闹钟、关闭采集专用标签页并写入空任务集合；历史快照和数据中心不受影响。采集历史页取消前 8 条截断，增加“清空全部历史”，只删除历史快照。
- 当前商品状态查询扩展到数据中心、历史 `itemsSnapshot` 和任务暂存结果；当前店铺状态同时参考数据中心和历史店铺快照。发现历史记录时按钮显示“重新采集”，点击仍走当前页面采集流程，不会复用历史数据。
- 导出链路继续传递业务文件名，并用 `downloads.onDeterminingFilename` 在 Chrome 最终确定文件名时再次覆盖建议名，避免系统保存对话框退回 UUID。业务规则仍为 `商品YYYYMMDD-HHmm.xlsx`、`店铺评价-店铺名-YYYYMMDD-HHmm.xlsx` 和 `商品表-店铺名-YYYYMMDD-HHmm.xlsx`。
- 图片在 offscreen 生成工作簿前使用规范化 URL、原始二进制和 32×32 视觉指纹做逐商品/逐评价去重，随后重排图序；`xlsx.js` 保留同规则兜底。这样单图商品不会因为多个 CDN 地址/尺寸变体产生图片 2、3，真实多图仍从表格末尾追加。

本轮新增/更新回归检查：

- 检查任务/历史清理消息和当前对象历史状态查询存在；
- 检查下载文件名模式与下载事件兜底监听；
- 检查不同 URL、不同二进制但视觉指纹一致的图片不会生成重复图片列；
- 重新运行商品、店铺、任务中心、停止任务和 UI 顺序烟测。

## 1.8 本轮 0.6.4：详情轮播图片根因修复与临时诊断功能移除

本轮针对“单图商品被写成图片 2/3/4、重复图片来自隐藏节点和 CDN 尺寸变体、用户不希望看到调试信息”做页面采集层修复。真实详情页确认：同一张图会同时出现在 `item-main-window-list` 缩略图、隐藏预览图和可见 `carouselItem` 中，URL 还会带 `220x`、`790x`、原图等不同后缀；旧实现把这些节点全部收集，再交给导出层处理，导致 Excel 出现重复图片列。

已完成：

- 新增 `image-utils.js` 作为隔离世界共享图片规则：只从详情 `item-main-window-carousel` 的真实 `carouselItem` 读取；单图轮播的 `only` 标记只保留一张逻辑主图。
- CDN 图片 URL 统一去除尺寸、质量、缩略图和跟踪参数，并识别 `.heic_220x10000Q90.jpg_.webp`、`.heic_Q90.jpg_.webp` 等闲鱼实际变体；同一图片候选只保留评分更高的原图/大图地址。
- 页面采集层排除缩略图、隐藏预览图、已售出/售罄遮罩和水印类图片；图片进入 offscreen/Excel 前就完成候选去重，不把重复判断推迟到 Excel 本地。
- `content.js` 的 DOM/API/二次扫描合并全部复用图片归一化规则，避免接口图片再次把已经去重的详情主图补回去。
- `offscreen.js` 同步扩展图片 URL 变体归一化和原图候选回退，优先尝试去掉 CDN 缩略后缀的高清地址。
- 移除侧边栏“导出页面诊断包”、`GET_PAGE_SNAPSHOT` 消息、诊断内容脚本和未使用的诊断压缩脚本；调试信息不再出现在插件界面、下载文件或运行时消息中。
- 新增 `tests/detail-image-utils-smoke.mjs`，覆盖同图不同尺寸、单图遮罩排除和真实多图保留；全部既有烟测继续通过。

本轮验证：

- 真实 Chrome 详情页确认单图商品 DOM 同时含缩略图、隐藏预览图和可见轮播图，且可见轮播项只有 1 个。
- 通过 JS 语法检查、详情图片规则烟测、商品/店铺导出烟测、任务并发、停止任务和 UI 顺序烟测。
- 当前无需用户再导出新的诊断包；如后续仍有问题，提供出错链接、截图和导出 Excel 即可。

## 1.9 本轮 0.6.5：店铺全商品四阶段采集与详情稳定会话

本轮针对“店铺公开有 12 个商品但只发现少量就结束”“页面主体字段尚未稳定加载却继续下一个商品”两个根因完成主分支修复：

- 店铺商品发现改为四阶段协议：先从账号页“宝贝”Tab 读取公开商品总数；再按视觉顺序滚动并持续扫描详情链接；只有发现数达到公开总数，或页面明确显示没有更多商品/宝贝，才进入详情阶段；最后对每条链接逐个建立详情加载会话。
- 店铺发现结果新增公开商品数、已发现数、是否完成和完成原因。公开总数为 12、实际只发现 4 条且没有明确“没有更多”时，任务会继续等待/重扫；达到截止条件仍不足时标记失败，不会把 4 条当成全部完成。页面明确没有更多但数量有差额时，任务继续采集已发现商品并以 `partial` 终态保留差额告警。
- 批量链接和店铺全部商品的每条详情都记录独立逻辑会话：目标 URL、开始时间、等待尝试次数、重新导航次数和最近错误。物理上继续复用任务专用标签页，避免打开大量标签，但每条商品都重新校验页面类型、详情 ID 和 URL。
- 详情页未稳定时不再使用固定“两次重试后跳过”的逻辑；当前链接在 45 秒会话截止时间内会继续处理登录遮罩、等待页面字段并在必要时重新导航当前 URL，只有截止后才记录失败并进入下一条。
- 登录提示兼容跨域 `alibaba-login-box` iframe：主页面看不到 iframe 内登录文字时，仍可识别可见登录弹窗外层并点击安全关闭区域，不填写登录凭据。

本轮新增回归检查：

- 公开商品数 12、已发现 4 条不能完成发现阶段；已发现 12 条可以进入详情阶段。
- 每个新详情链接都会创建新的 `detailSession`，旧商品的等待次数不会带到下一商品。
- 详情/店铺/任务中心/停止任务/导出图片既有烟测继续通过。

## 2. 已完成

### 数据采集基础

- [x] Manifest V3 基础结构。
- [x] 当前详情页 DOM 采集骨架。
- [x] 商品字段、店铺字段、评价字段和图片 URL 的规范化。
- [x] 商品去重和本地扩展存储。
- [x] 搜索页只发现链接，最终结果由详情页采集的守卫逻辑。
- [x] 批量商品链接逐个导航。
- [x] 搜索跨页状态机：目标商品数、最大页数、页码/分页控件推进、失败重试、停止任务。
- [x] 旧页面补注入 content/main 脚本的兼容逻辑。
- [x] 根据详情页诊断包适配主文案、主图轮播区、卖家信息区和“来闲鱼 X 年”时长。
- [x] 根据搜索页诊断包适配 `resultList[].data.item.main.exContent`，过滤页面配置资源误报。
- [x] 识别 `/personal?userId=...` 账号页，读取店铺简介、粉丝/关注、商品数和公开评价。
- [x] 商品任务与店铺任务分离；批量/搜索详情完成后自动进入卖家账号页补充商品行的基础店铺字段，完整评价仍由店铺页任务单独采集。
- [x] 账号页优先读取资料区的店铺名称，不再使用页面顶部“登录”等通用昵称节点。
- [x] 店铺页自动进入评价区域并滚动触发懒加载，保存逐条评价文本、时间/地区和评价图片对象。
- [x] 店铺资料、评价和评价图片使用独立本地存储，导出时分别写入不含商品主表的“店铺资料”和“店铺评价综合”工作簿数据表。
- [x] 详情接口观察结果按当前商品 ID/详情 URL过滤，避免相关推荐或其它详情 API 记录混入商品主表。
- [x] 当前详情单独导出沿用后台过滤后的商品结果；旧任务缺少暂存字段时保留商品导出兼容入口。

### 导出基础

- [x] 生成 `.xlsx`。
- [x] 商品多图与商品字段同表嵌入，不再生成商品图片索引表。
- [x] 下载真实图片并嵌入工作簿，而不只输出图片链接。
- [x] 图片名称组合商品标题、店铺名、商品 ID 和序号。
- [x] 图片下载失败时继续导出其它数据。
- [x] 商品主表增加卖家账号页、地区、粉丝数、关注数、卖家商品数等商品详情补充字段；评价正文和评价图片使用独立店铺评价表。
- [x] 商品主表调整为用户指定的 20 列和顺序，新增商品文案后的浏览数、想要数，移除商品标题、图片状态、来源页面、数据来源等辅助列。
- [x] 店铺资料表移除无法由店铺页可靠读取的开店时长、商品好评率，禁止用评价数量推算好评率。
- [x] 店铺导出将资料写入“店铺资料”表、评价和评价图片写入“店铺评价综合”表；评价图片使用真实二进制嵌入对应评价行。
- [x] 商品图和评价图分开排队；商品图片上限不会截断已采集的评价图片，异常页面另有评价图片保护上限。

### 已做检查

- [x] 通过 `node --check background.js content.js popup.js xlsx.js main-world.js`。
- [x] 通过 manifest JSON 解析。
- [x] 通过搜索跨页模拟：目标 3 条、两页搜索，最终访问的都是 `/item` 详情链接。
- [x] 通过 xlsx ZIP 检查：导出包中包含真实图片媒体文件和 drawing。
- [x] 【历史 0.5.x 回归记录】通过 xlsx ZIP 检查：当时版本同时包含 6 个工作表、商品/评价图片媒体文件和 3 类 drawing；当前版本的工作簿结构以 0.6.0 文档和现行烟测为准。
- [x] 【历史 0.5.x 回归记录】通过 xlsx ZIP 检查：当时商品工作簿为 6 张表、店铺工作簿为 4 张独立店铺表；当前版本商品工作簿为“商品数据/说明”，店铺工作簿为“店铺资料/店铺评价综合”。
- [x] 通过静态语义回归：`1天前来过 | 来闲鱼239天` 只能得到 `239天`；服务类型优先进入唯一类目字段。
- [x] 通过 `tests/product-export-shape-smoke.mjs` 与 `tests/store-export-shape-smoke.mjs`：商品表的浏览数/想要数顺序和值正确，店铺表不再输出开店时长、商品好评率或派生百分比。
- [x] 通过 `tests/task-center-concurrency-smoke.mjs`：两个任务并发写入不会互相覆盖；店铺全商品任务的详情结果进入自己的暂存区。

## 停止任务回归检查

- [x] 通过 `tests/stop-job-smoke.mjs`：停止后状态保持 `stopped`、任务标签页被关闭、闹钟不会重建，旧闹钟不能恢复任务。

## 3. 本次正在开发

### P0：已完成代码实现，待真实浏览器验收

- [x] 侧边栏主界面，按“主页—功能页—处理任务—数据中心”分层。
- [x] 参考社媒助手的白底卡片、分组入口、返回导航、任务进度环和完成态布局。
- [x] 处理任务页显示任务类型、采集模式、创建时间、进度、失败数和导出入口。
- [x] 移出扩展根目录中的 `_diagnostic-review-20260806` 诊断副本，恢复 Chrome Reload 能力。
- [x] 统一 RPA 页面详情模式和 API 接口观察模式。
- [x] 自动下载/手动下载设置。
- [x] 任务完成通知、失败通知和状态 badge。
- [x] 修复停止任务竞态：取消在途异步流程、清除任务闹钟、关闭采集专用标签页，并保留已采集结果。
- [x] 解决按钮点击无响应时的恢复路径和可见错误。
- [x] 修复搜索页无文字“下一页”按钮时的页码推进；兼容闲鱼 `search-page-tiny-container` 分页器。
- [x] 修复搜索按钮被 `GET_JOB_STATUS` 轮询覆盖为禁用的问题，按钮现在按当前 `pageType` 保持可用状态。
- [x] 将返回导航改为单层父级关系，避免详情页与下载设置页来回跳转。
- [x] 将首页说明卡片移动到功能入口之后。
- [x] 搜索页空列表改为异步渲染重试；没有成功进入详情页时标记为失败，禁止“完成 0 条”的假成功。
- [x] 搜索详情阶段增加页面类型、待采集链接和商品 ID 校验。
- [x] 搜索页商品链接按可见卡片屏幕坐标逐排排序；接口缓存只按商品 ID 补字段，不再追加 network-only 商品或改变视觉顺序。
- [x] 当前详情、批量、搜索和账号页增加公开登录提示关闭与页面稳定等待；不填写登录信息，不把骨架屏/登录提示当作字段。
- [x] 当前详情、批量任务和搜索任务使用结果暂存区，完成后提供加入数据中心与独立导出两个按钮。
- [x] 当前详情成功只接受真实返回商品行；按商品 ID 保留暂存结果，同商品跟踪参数变化和账号页往返不再使结果按钮变灰。
- [x] 当前详情页补充卖家资料时复用原商品标签页，并在读取完成后返回原详情页，不创建额外卖家标签页。
- [x] 批量/搜索任务最后一条商品也必须执行 `account-page` 补采集阶段后才允许结束。
- [x] 商品/店铺工作簿完全分离；首页数据中心提供商品表和店铺表两个页签及独立下载入口。
- [x] 商品图片列使用真实图片 drawing，商品多图全部进入商品表同一行；评价图片按店铺、评价序号、图片序号去重并嵌入评价综合对应行。

### P1：已完成代码实现，待真实数据验收

- [x] 下载设置：相对文件夹、按数据类型固定命名、另存为开关、图片上限。
- [x] 历史任务列表、查看结果、重新导出、删除。
- [x] 任务结束后的导出记录和重复下载保护。
- [x] API 网络响应短期缓冲，确保在页面请求先发生时仍可捕获。
- [x] 历史诊断包适配完成；运行时诊断导出入口、诊断消息和临时诊断脚本已在 0.6.4 移除。
- [x] 按评价诊断包适配 `rateList--*`、`rateItem--*`、`feedback--*`、`timeIp--*`、`rateImg--*` 等结构。
- [x] 增加店铺页侧边栏入口和完成提示，显示已读取评价条数。
- [x] 识别评价列表内部可滚动容器，滚动触发更多评价；主表优先使用同商品的第一张成功图片。
- [x] 好评率、粉丝数、关注数和店铺评价数增加字段语义校验；店铺简介不做数字格式限制，只接受账号资料区域内的明确简介节点。
- [x] 店铺页采集完成后直接显示“立即下载 Excel”和“查看数据中心”。
- [x] 当前店铺页按卖家账号显示“首次可采集”或“历史已采集”、评价数量和最近采集时间；重新采集显示历史更新结果。
- [x] 店铺 Excel 默认打开“店铺资料”，下载完成提示明确两张表、评价条数和已嵌入图片数。
- [x] 详情属性区兼容“服务类型”拆字标签；只有内部 `categoryId` 时不再把编号写入类目。
- [x] DOM/API/二次扫描的非空字段合并，避免空值覆盖有效值；店铺资料改为独立采集和独立导出，不再由商品任务隐式补充。
- [x] 修复账号页读取过早、整页 `body` 宽泛查找和跨 Tab `.num--` 串位；账号页诊断包增加 `account-profile.json` 解析快照。
- [x] 后台 `tabs.sendMessage`、侧边栏页面消息和内容脚本运行时消息增加 12 秒超时，避免页面无响应时按钮无限等待。
- [x] `START_API_CAPTURE` 和当前详情采集的异步回调增加 `try/catch`，任何解析异常都会结束消息并反馈错误。
- [x] 每个任务读取增加 90 秒无进展恢复：旧 `collecting`/`waiting-page` 任务只释放自身闹钟/标签页，旧 `ready-to-collect` 任务重新安排闹钟，不影响其它任务。
- [x] 任务中心保存多个并行任务，支持指定任务查看、暂停/继续、停止、提交商品表和导出。
- [x] 店铺全部商品任务：店铺页发现商品链接后逐个进入详情页，并使用同一任务暂存商品结果。
- [x] 长文案展开、原图优先下载、商品多图同表嵌入和商品/店铺/评价三类字段工作台。

### 0.6.0 监控功能实现状态

- [x] 新建 `codex/monitoring` 分支和独立工作目录，稳定版 `main` 目录不随本轮开发改动。
- [x] 商品监控、店铺监控两个入口和监控配置表单。
- [x] 多链接校验：商品入口只接受商品详情链接，店铺入口只接受店铺/账号页链接。
- [x] 每日时间、相对子文件夹、采集模式和启用状态保存到 `xianyu_monitor_configs_v1`。
- [x] 独立监控运行状态 `xianyu_monitor_runs_v1`，逐条处理、失败记录、进度展示和独立闹钟。
- [x] 商品监控复用详情页采集和卖家基础资料补采，自动导出商品工作簿。
- [x] 店铺监控复用店铺资料、公开评价和评价图片采集，自动导出两张店铺工作表。
- [x] 支持立即运行、编辑、暂停/启用、删除；完成/部分失败/失败会更新列表并发送系统通知。
- [ ] 尚未在真实 Chrome 登录闲鱼环境等待完整一轮定时监控；需要验收标签页是否保持非激活、登录提示是否能关闭、图片权限和下载目录是否符合预期。

## 4. 当前风险

1. 闲鱼详情页路由和 API 响应结构可能变化，需要真实浏览器验收后补充适配器。
2. 某些图片 CDN 会拒绝扩展上下文的无凭据请求，可能需要使用页面已公开可读的响应或在可控范围内回退到页面上下文下载。
3. 浏览器扩展不能任意设置绝对下载目录；设置采用浏览器下载目录下的相对文件夹。
4. 接口观察模式只保证“观察页面已公开收到的数据”，不保证接口响应含有店铺简介、开店时长和评价全部字段；现在会按当前商品 ID过滤响应，店铺字段缺失时应单独使用“采集当前店铺评价”。
5. 账号页公开评价需要页面实际加载到浏览器；如果闲鱼要求登录、延迟加载或临时限制，店铺页会显示已读取数量，不能把服务器显示的总评价数当成已成功读取数。
6. 店铺页评价图片可能使用 CDN 原图与缩略图两套地址，采集器优先使用评价内容的原图节点；CDN 拒绝扩展程序访问时会在“店铺评价综合”表的图片状态和失败地址字段显示原因。
7. 当前账号页诊断包来自其它卖家或旧版本页面时，不能证明新出现的字段串位已经覆盖；若修复后仍有错误，需要从实际出错店铺页重新导出诊断包，重点查看 `account-profile.json`。
8. 监控自动下载只能使用 Chrome 默认下载目录下的相对子文件夹；如果 Chrome 被关闭、账户退出或页面公开内容需要人工登录，监控会记录失败并等待下一次计划，不会绕过登录或风控。
9. 闲鱼详情轮播节点和 CDN 图片后缀可能变化；0.6.4 已将图片候选选择集中到 `image-utils.js`，后续若仍有重复图片，优先核对出错链接的轮播结构和导出 Excel，不再要求用户导出调试包。

## 5. 下一步验收顺序

1. 重新加载扩展，在已经打开的闲鱼详情页打开侧边栏，测试当前页采集。
2. 测试接口观察模式，确认 `dataSource` 能标出 network 来源。
3. 在搜索结果页启动 10 条/最多 2 页，确认按钮可点击，专用标签页按页码自动逐个进入详情页；若把采集专用标签页人为停在搜索页，任务应重试并最终失败，不应完成 0 条。
4. 任务结束后测试自动下载和完成提示。
5. 切换手动下载，确认结果保留且侧边栏出现下载按钮。
6. 打开历史记录，重新导出并检查图片名称与 Excel 内嵌图片。
7. 跑批量/搜索任务，确认每条详情后都显示“正在打开卖家账号页”，最后一条也完成账号页阶段，并把简介、开店时长、粉丝/关注和评价数写入商品行。
8. 打开店铺/账号页，点击“采集当前店铺数据”，确认评价区自动加载、店铺资料写入暂存区、评价数量可见；确认出现圆形完成状态，再分别测试“立即下载店铺 Excel”和“加到数据中心店铺表”，导出后检查“店铺资料”表中的资料字段，以及“店铺评价综合”表中的评价字段和评价图片。
9. 在监控页分别保存商品监控和店铺监控，确认多链接、每日时间、相对下载文件夹、启用状态在重开侧边栏后仍保留。
10. 点击监控列表的“立即运行”，确认每条链接都进入正确页面类型、专用标签页不激活、完成后自动关闭并下载对应类型工作簿；运行期间从普通首页仍能点击当前详情、批量和搜索入口。

## 7. 本轮自动验收记录

- [x] 通过全部 JS 语法检查和 manifest 文件引用检查。
- [x] 通过 xlsx ZIP 检查：商品表存在 `xl/media/*`、`xl/drawings/*`，并确认不生成商品图片索引 sheet。
- [x] 通过 offscreen 空数据导出检查：能生成可下载的 Blob URL。
- [x] 通过后台消息检查：API 模式、任务停止、结果键写入正常。
- [x] 通过搜索状态机模拟：目标 3 条、最多 2 页，实际访问 3 个详情 URL，最终成功完成。
- [x] 【历史回归记录】诊断 ZIP 生成和回放曾用于 0.5.x/0.6.0 页面适配；0.6.4 已从插件运行时移除，不再作为用户功能。
- [x] 通过详情 DOM 结构模拟检查：主标题、长文案、价格、主图、卖家昵称、卖家链接和“来闲鱼 6 年”字段均可进入详情记录。
- [x] 使用真实账号页诊断 DOM 离线回归：地区、粉丝、关注、简介、商品数和店铺评价数均从正确资料区域读取；使用拆字详情属性 DOM 回归得到 `类目=金融`。
- [x] 根据真实搜索诊断 DOM 确认分页器结构为 `search-page-tiny-container`，并加入页码/右箭头识别逻辑。
- [x] 复核上传的 Excel：确认原文件为 24 列，发现卖家名称为“登录”和 3 条只含标题/链接的 `network:detail` 记录；代码已针对这两类问题增加账号资料优先级和详情 ID 过滤。
- [x] 复核评价诊断包：确认账号页可见 `火花数研`、广东省、110 粉丝、1 关注、4 件宝贝、174 条店铺评价、151 个评价节点和评价图片节点；已对应到店铺采集器。
- [x] 通过模拟导出检查 18 列商品主表和店铺评价综合表的真实图片 drawing。
- [x] 【历史 0.5.x 回归记录】通过失败商品图 + 成功商品图 + 评价图的导出回归检查：当时验证了 6 个工作表、3 个 drawing、2 个真实媒体文件；当前版本改由商品/店铺导出烟测分别验证同表嵌入。
- [x] 通过代码级回归检查：纯数字简介在 content、background、xlsx 三层均不再被格式过滤；账号页读取增加稳定快照和资料区域作用域。
- [x] 通过后台隔离模拟：旧 `collecting` 任务在读取状态时转为 `failed`，只释放自身资源；页面消息超时和 API 异常回调通过静态检查。
- [x] 复核 `1.xlsx`：商品数据仅 4 行，商品 ID 顺序与当前搜索页第一排从左到右的可见顺序不一致；真实 Chrome DOM 检查确认当前搜索页 30 张卡片本身具有正确逐排顺序，根因在扩展合并/排队顺序而非闲鱼页面。
- [x] 复核 `2.xlsx`：独立店铺工作簿实际包含 78 条评价记录、5 个评价图片对象和 5 个嵌入媒体文件；0.5.9 将店铺资料与评价综合重新按两张表组织，避免资料字段和评价行混杂。
- [x] 通过 `tests/ui-order-smoke.mjs`：乱序卡片按两行视觉坐标恢复为左到右、逐排向下；当前详情真实返回一行后两个结果按钮保持可用。
- [x] 通过 `tests/store-export-shape-smoke.mjs`：店铺工作簿严格生成“店铺资料”“店铺评价综合”两张表，1 条店铺资料、2 条模拟评价各占一行，3 张评价图片嵌入评价综合对应评价行。
- [x] 通过任务状态机回归：详情阶段完成后进入账号阶段，`239天` 和纯数字简介 `12345` 能合并到商品暂存行，最后一条完成账号阶段后任务才结束。
- [ ] 尚未替代用户在真实 Chrome 闲鱼登录环境完成浏览器验收；本轮已完成代码级和离线工作簿回归，但仍需在真实页面确认详情/账号 DOM、公开接口字段、CDN 图片权限，以及搜索任务是否逐个进入详情和店铺页并在完成后正确显示两个结果按钮。

## 8. 版本出口标准

当 P0 全部勾选、P1 中的设置/历史/接口缓冲完成，并通过开发文档中的静态检查、模拟检查和浏览器验收后，版本从“开发中”改为“可试用”。
